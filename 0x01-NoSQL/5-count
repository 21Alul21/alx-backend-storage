#!/bin/bash

# Extract the database name from the command line argument
DB_NAME=$1

# Execute the MongoDB command to count the documents in the "school" collection
mongo $DB_NAME --eval 'db.school.countDocuments()'

